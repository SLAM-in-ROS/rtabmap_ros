name: rtabmap-ros

distro: melodic

build: catkin_make

runtime: nvidia

packages:
  - ros-melodic-rtabmap-ros
  - ros-melodic-find-object-2d

volume:
  - host_path: /DATASET
    container_path: /DATASET
    options: ro
